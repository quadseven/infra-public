# parity-case: U - the bounds appear only in a comment on a continuation line
# canonical-rule5-lines: 9
jobs:
  x:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: |
          curl -fsS \
            https://example.invalid/  # --max-time 10 --connect-timeout 5
